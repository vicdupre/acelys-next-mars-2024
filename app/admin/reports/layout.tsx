import { LayoutProps } from "@/app/lib/types";

const AdminReportsLayout = ({ children }: LayoutProps) => {
  return <div style={{ padding: "20px" }}>{children}</div>;
};

export default AdminReportsLayout;
